package com.ats.qosmpp.web;




public class AdminRessource {
/*

    private RequestDataRepository requestDataRepository;
    private ResponseDataRepository responseDataRepository;

    public AdminRessource(RequestDataRepository requestDataRepository, ResponseDataRepository responseDataRepository) {
        this.requestDataRepository = requestDataRepository;
        this.responseDataRepository = responseDataRepository;
    }

    @RequestMapping(value = "/requests-data", method = RequestMethod.GET)
    public DataTablesOutput<Requests> list(@Valid @RequestBody DataTablesInput input) {
        return requestDataRepository.findAll(input);
    }

    @RequestMapping(value = "/requests-data", method = RequestMethod.POST)
    public DataTablesOutput<Requests> listReq(@Valid @RequestBody DataTablesInput input) {
        return requestDataRepository.findAll(input);
    }

    @RequestMapping(value = "/responses-data", method = RequestMethod.GET)
    public DataTablesOutput<Responses> listResp(@Valid @RequestBody DataTablesInput input) {
        return responseDataRepository.findAll(input);
    }

    @RequestMapping(value = "/responses-data", method = RequestMethod.POST)
    public DataTablesOutput<Responses> listPOST(@Valid @RequestBody DataTablesInput input) {
        return responseDataRepository.findAll(input);
    }
*/

}
